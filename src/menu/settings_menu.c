/*
** EPITECH PROJECT, 2020
** my defender
** File description:
** settings_menu
*/

#include "my.h"

void settings_menu(utils_t *utils, settings_t *set)
{
    while (utils->settings == true) {
        while (sfRenderWindow_pollEvent(utils->window))
    }
}